testTuple = ("apple", "banana", "strawberry")
print(testTuple)
# orderd- cant change order of the items
# cant change items
# allow duplicates
# zero-based

# Unpack Tuple
(green, yellow, red) = testTuple
print(green, yellow, red)

# count number of 1's
print((1, 2, 1, 1, 2, 1).count(1))
